namespace LibraProgramming.Xaml.Parsing.StateMachine
{
    /*public static class StateMachineExtension
    {
        public static ICommand CreateCommand<TState, TTrigger>(this ParsingState<TState, TTrigger> machine,
            TTrigger trigger)
            where TState : struct
        {
            return new RelayCommand(
                () => machine.Fire(trigger),
                () => machine.CanFire(trigger)
                );
        }
    }*/
}